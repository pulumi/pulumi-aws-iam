{
    "name": "@pulumi/aws-iam",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource aws-iam ${VERSION}"
    },
    "dependencies": {
        "@pulumi/aws": "^5.0.0",
        "@pulumi/pulumi": "^3.42.0"
    },
    "devDependencies": {
        "@types/node": "^14",
        "typescript": "^3.7.0"
    },
    "pulumi": {
        "resource": true,
        "name": "aws-iam"
    }
}
